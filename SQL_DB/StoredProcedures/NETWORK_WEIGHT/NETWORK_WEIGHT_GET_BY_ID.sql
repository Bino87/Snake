CREATE PROCEDURE [dbo].[NETWORK_WEIGHT_GET_BY_ID]
	@ID INT
AS
BEGIN
SELECT A.ID,A.INTERNAL_INDEX,A.LAYER_ID,B.VALUE  FROM [dbo].NETWORK_WEIGHT A
LEFT JOIN NETWORK_VALUES B
ON A.VALUE_ID = B.ID
WHERE A.ID = @ID
END
